---
- module_defaults:
    group/infoblox.bloxone.all:
      csp_url: "{{ csp_url }}"
      api_key: "{{ api_key }}"
  block:
    - name: "Delete Infra Host"
      infoblox.bloxone.infra_host:
        name: "{{ display_name }}"
        state: "absent"
      ignore_errors: true